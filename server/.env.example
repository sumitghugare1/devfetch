# Backend Environment Variables
# Copy this to .env for local development

# 📱 LOCAL STORAGE SETUP (No Database Required!)
# The frontend now uses localStorage for request history
# No MongoDB setup needed - everything works locally in the browser!

# MongoDB Connection URI - OPTIONAL (Legacy support)
# Leave empty or comment out if you want to run without database
# MONGODB_URI=mongodb://localhost:27017/devfetch

# For production MongoDB Atlas (OPTIONAL)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/devfetch

# Server Port
PORT=5000

# Environment
NODE_ENV=development

# ✅ SIMPLIFIED DEPLOYMENT:
# 1. No database setup required
# 2. Request history stored locally in user's browser
# 3. Zero configuration needed for MongoDB
# 4. Just deploy and it works!
